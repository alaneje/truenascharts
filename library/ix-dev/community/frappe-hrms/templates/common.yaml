{{- include "ix.v1.common.loader.init" . -}}

{{/* Merge the templates with Values */}}
{{- $_ := mustMergeOverwrite .Values (include "frappeHrms.service" $ | fromYaml) -}}
{{- $_ := mustMergeOverwrite .Values (include "frappeHrms.persistence" $ | fromYaml) -}}
{{- $_ := mustMergeOverwrite .Values (include "frappeHrms.workload" $ | fromYaml) -}}

{{/* Create the configmap for portal manually*/}}
{{- include "frappeHrms.portal" $ -}}

{{- include "ix.v1.common.loader.apply" . -}}
