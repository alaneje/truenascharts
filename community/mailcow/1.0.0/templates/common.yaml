{{- include "ix.v1.common.loader.init" . -}}

{{/* Merge the templates with Values */}}
{{- $_ := mustMergeOverwrite .Values (include "mailcow.configuration" $ | fromYaml) -}}
{{- $_ := mustMergeOverwrite .Values (include "mailcow.service" $ | fromYaml) -}}
{{- $_ := mustMergeOverwrite .Values (include "mailcow.persistence" $ | fromYaml) -}}
{{- $_ := mustMergeOverwrite .Values (include "mailcow.workload" $ | fromYaml) -}}
{{- $_ := mustMergeOverwrite .Values (include "mailcow.mariadb.workload" $ | fromYaml) -}}
{{- $_ := mustMergeOverwrite .Values (include "mailcow.redis.workload" $ | fromYaml) -}}

{{/* Create the configmap for portal manually*/}}
{{- include "mailcow.portal" $ -}}

{{- include "ix.v1.common.loader.apply" . -}}
